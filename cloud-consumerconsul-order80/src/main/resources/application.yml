server:
  port: 80

spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: 115.29.174.8
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        heartbeat:
          enabled: true

